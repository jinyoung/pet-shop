apiVersion: v1
kind: Service
metadata:
  name: supplier-management
  labels:
    app: supplier-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: supplier-management